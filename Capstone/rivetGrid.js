'use strict';

console.log('working');
const menu = document.querySelectorAll('.rvt-dropdown__menu > button');

console.log(menu);
// if button is in the menu and is clicked 
//add text content into search bar

menu.forEach()